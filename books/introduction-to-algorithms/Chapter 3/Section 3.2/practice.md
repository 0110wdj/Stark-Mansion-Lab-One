## 3.2 practice

### 3.2-1 证明：若 f(n) 和 g(n) 是单调递增的函数，则函数 f(n)+g(n) 和 f(g(n)) 也是单调递增的，此外，若 f(n) 和 g(n) 是非负的，则 f(n)⋅g(n) 是单调递增的。

根据函数单调性定义：

$$
若 m \leq n 蕴含 f(m) \leq f(n),则函数 f(n) 是单调递增的。
$$

证明：

已知 f(n) 和 g(n) 是单调递增的函数，则存在 m ≤ n 使得 f(m) ≤ f(n), g(m) ≤ g(n)。

显然 f(m)+g(m) ≤ f(n)+g(n)

**则有函数 h(n) = f(n)+g(n) 为单调递增函数(证明 1)**

令 x = g(m), y = g(n)
存在 g(m) ≤ g(n) 即 x ≤ y
对 f(x) ≤ f(y) 成立
带入得
f(g(m)) ≤ f(g(n)) 对 m ≤ n 成立

**则有函数 h(n) = f(g(n)) 为单调递增函数(证明 2)**

f(m) ≤ f(n)
f(m)⋅g(n) ≤ f(n)⋅g(n)
f(m)⋅g(n) ≤ f(n)⋅g(n)

g(m) ≤ g(n)
g(m)⋅f(m) ≤ g(n)⋅f(m)

**综上 f(m)⋅g(m) ≤ f(n)⋅g(n) (证明 3)**

### 3.2-2 证明等式(3.16)

等式(3.16)原文：

$$
a^{log_bc} = c^{log_ba}
$$

证明：
使用的基本性质：

$$
log_b(a^n) = n⋅log+b(a)\\
log_ba=\frac{log_ca}{log_cb}
$$

证明过程：

$$
log(a^{log_bc}) = log_bc⋅log(a)\\
log(c^{log_ba}) = log_ba⋅log(c)\\
需证明 log_bc⋅loga = log_ba⋅logc\\
\frac {log_bc}{log_ba}=\frac {logc}{loga}\\
log_ac =log_ac\\
$$

证毕

### 3.2-3 证明等式(3.19)。并证明 \(n! = ω(2^n)\) 且 \(n! = ο(n^n)\)

等式(3.19)原文：

$$
lg(n!) = Θ(nlgn)
$$

证明 1：

根据 n! 的定义：n! = 1⋅2⋅3⋅⋅⋅n，可得：（当 n > 1 时成立）
\(2^n\) = 2⋅2⋅2⋅⋅⋅2 ≤ n! ≤ n⋅n⋅n⋅⋅⋅n = \(n^n\)

求除法极限：

$$
\lim_{x \to n} \frac {2^n}{n!} = \frac {2⋅2⋅2⋅⋅⋅2}{1⋅2⋅3⋅⋅⋅n} = 2⋅1⋅\frac {2}{3}⋅⋅⋅\frac {2}{n-1}⋅\frac {2}{n}  = 0 \\
---- \\
---- \\
\lim_{x \to n} \frac {n^n}{n!} = \frac {n⋅n⋅n⋅⋅⋅n}{1⋅2⋅3⋅⋅⋅n} = n⋅\frac {n}{2}⋅\frac {n}{3}⋅⋅⋅⋅\frac {n}{n-1}⋅1  = \infty \\
$$

**证得 \(n! = ω(2^n)\) 且 \(n! = ο(n^n)\)**

证明 2：

先列出定义：

$$
Θ(g(n))={f(n):存在正常量 c_1、c_2 和 n_0，使得对所有 n \geq n_0，有 0 \leq c_1g(n) \leq f(n) \leq c_2g(n)}
$$

需要证明:

$$
c_1nlgn ≤ lg(n!) ≤ c_2nlgn\\
即 nlgn^{c_1} ≤ lg(n!) ≤ nlgn^{c_2}\\
$$

证明：

$$
已知 n! = ω(2^n) 且 n! = ο(n^n)\\
则有 2^n < n! < n^n\\
根据函数单调性 lg(2^n) < lg(n!) < lg(n^n)\\
即 nlg2 < lg(n!) < nlgn
$$

显然，存在 \(c_1\) 和 \(c_2\) 使得下面不等式成立：

$$
nlgn^{c_1} ≤ lg(n!) ≤ nlgn^{c_2}
$$

证毕(存在疑惑)

### 3.2-4 函数 \( ⌈ lgn ⌉ !\) 多项式有界吗？函数 \( ⌈ lglgn ⌉ !\) 多项式有界吗？

根据网上获取到的定义：

函数 f(x) 有界：
对于函数 f(x)，如果存在实数 M 和 N，使得对于所有的 x 在定义域内，都有 M ≤ f(x) ≤ N，那么这个函数就被称为有界的。

函数 f(x) 多项式有界：
这存在一个多项式 P(x) 和正实数 M，使得对于所有 x 大于某个阈值 N，都有 \(∣⌈logx⌉!∣ ≤ P(x)\)，即 \(∣⌈logx⌉!∣\)在某个点之后始终受到某个多项式 P(x) 的界限。

**没有找到可靠的信息，此题略过。**

(根据[官方参考答案](http://mitp-content-server.mit.edu:18180/books/content/sectbyfn?collid=books_pres_0&id=8030&fn=Intro_to_Algo_Selected_Solutions.pdf)，\( ⌈ lgn ⌉ !\)无界， \( ⌈ lglgn ⌉ !\) 有界。细节以后补充。)

### 3.2-5 如下两个函数中，哪一个渐近更大些：\(lg(lg^∗n)\) 还是 \(lg^∗(lgn)\)?

直接展开看看：

\(lg(lg^∗n)\)
= \(lg(lg(lg(..lg(2)..)))\)

**n+1 个 lg**

\(lg^∗(lgn)\)
=\(lg(lg(..lg((lgn))..))\)

**n+1 个 lg**

显然，都是递增函数，下面的式子渐近更大一些。

### 3.2-6 证明：黄金分割率 \(\phi\) 及其共轭数 \(\hat{\phi}\) 都满足方程 \(x^2=x+1\) 。

求解方程得：
\(x_1=\frac{1+\\\sqrt5}{2}\)
\(x_2=\frac{1-\\\sqrt5}{2}\)

根据黄金分割率的定义：～。

证明完成。（存疑）

### 3.2.7 用归纳法证明：第 i 个斐波那契数满足等式 \(F_i=\frac{\phi^i - \hat{\phi}^i}{\sqrt 5}\)，其中 \(\phi\) 是黄金分割率且 \(\hat{\phi}\) 是其共轭数。

**证明：**

显然 \(F_0 = 0, F_1 = 1\)

第一步（基础情况）:
我们首先证明当 n = 2 时,

左侧：
\(F_2 = 1\)

右侧：
\(\frac{\phi^i - \hat{\phi}^i}{\sqrt 5}\)
= \(\frac{\phi^2 - \hat{\phi}^2}{\sqrt 5}\)
= \(\frac{(\frac{1+\\\sqrt5}{2})^2 - (\frac{1-\\\sqrt5}{2})^2}{\sqrt 5}\)
= \(\frac{(\frac{1+\\\sqrt5}{2}-\frac{1-\\\sqrt5}{2} )(\frac{1+\\\sqrt5}{2}+\frac{1-\\\sqrt5}{2})}{\sqrt 5}\)
= 1

基础情况成立。

第二步(归纳假设):
假设对于第 k 个数子，即 i = k 时等式成立，则有 \(F_k = \frac{\phi^k - \hat{\phi}^k}{\sqrt 5}\) 成立。

第三步(归纳证明):
我们需要证明当 i = k + 1 时，

$$
F_{k+1} = \frac{\phi^{k+1} - \hat{\phi}^{k+1}}{\sqrt 5}
$$

能够成立。

根据斐波那契数列定义可得：

$$
F_{k+1} = F_k + F_{k-1} = \frac{\phi^{k} - \hat{\phi}^{k}}{\sqrt 5} + \frac{\phi^{k-1} - \hat{\phi}^{k-1}}{\sqrt 5} = \frac{\phi^k + \phi^{k-1} - \hat{\phi}^k - \hat{\phi}^{k-1}}{\sqrt 5} = \frac{(\phi + 1)(\phi^{k-1}) - (\hat{\phi} + 1)(\hat{\phi}^{k-1})}{\sqrt 5}
$$

根据定义：\(\phi\) 和 \(\hat{\phi}\) 是方程 \(x^2 = x + 1\) 的两个根，所以有：

$$
\phi^2 = \phi + 1\\
\hat{\phi}^2 = \hat{\phi} + 1
$$

带入上式继续计算：

$$
F_{k+1} = \frac{(\phi^2)(\phi^{k-1}) - (\hat{\phi}^2)(\hat{\phi}^{k-1})}{\sqrt 5} = \frac{\phi^{k+1} - \hat{\phi}^{k+1}}{\sqrt 5}
$$

证明完成。

### 3.2.8 证明：klnk = Θ(n) 蕴涵着 k = Θ(n/lnn)。

根据定义：

$$
Θ(g(n))={f(n):存在正常量 c_1、c_2 和 n_0，使得对所有 n \geq n_0，有 0 \leq c_1g(n) \leq f(n) \leq c_2g(n)}
$$

有：
\(c_1g(n) ≤ f(n) ≤ c_2g(n) \)
\(c_1n ≤ klnk ≤ c_2n \)
**对任意 k 成立**

令 \(k = \frac{n}{lnn}\)，下式也成立：
\(c_1n ≤ \frac{n}{lnn}ln(\frac{n}{lnn}) ≤ c_2n \)
\(c_1 ≤ \frac{1}{lnn}ln(\frac{n}{lnn}) ≤ c_2 \)
\(c_1lnn ≤ ln(\frac{n}{lnn}) ≤ c_2lnn \)
\(c_1lnn ≤ lnn - lnlnn ≤ c_2lnn \)
\(c_1 ≤ 1 - \frac{lnlnn}{lnn} ≤ c_2\)
\(c_1 + \frac{lnlnn}{lnn} ≤ 1 ≤ c_2 + \frac{lnlnn}{lnn} \)

需证明：
\(\frac{c_3n}{lnn} ≤ k ≤ \frac{c_4n}{lnn}\)
\(\frac{c_3n}{lnn} ≤ \frac{n}{lnn} ≤ \frac{c_4n}{lnn}\)
\(c_3 ≤ 1 ≤ c_4\)

**关键步骤：**
当 n 足够大时，\( \frac{lnlnn}{lnn} \) 趋紧于 0，所以存在约等于 \(c_3 = c_1 + \frac{lnlnn}{lnn} \) 常数，使得需证明的不等式链左侧成立。右侧同理。（存疑）

## think

### 3-1 (多项式的渐近行为)

假设

$$
p(n) = \lim_{i=0 \to d}a_in^i
$$

是一个关于 n 的 d 次多项式，其中 \(a_d > 0\), k 是一个常量。

使用渐近记号的定义来证明下面的性质。

a.若 k ≥ d，则 \(p(n) = Ο(n^k)\)。
b.若 k ≤ d，则 \(p(n) = Ω(n^k)\)。
c.若 k = d，则 \(p(n) = Θ(n^k)\)。
d.若 k > d，则 \(p(n) = ο(n^k)\)。
e.若 k < d，则 \(p(n) = ω(n^k)\)。

证明：

- a

根据 Ο 定义：

$$
Ο(g(n))={f(n):存在正常量 c 和 n_0，使得对所有 n \geq n_0，有 0 \leq f(n) \leq cg(n)}
$$

需要证明：
\(f(n) ≤ cg(n)\)
\(p(n) ≤ cn^k\)
\(Ο(n^d) ≤ cn^k\)

已知：
\(k ≥ d\)
\(n^k ≥ n^d\)
\(Ο(n^k) ≥ Ο(n^d)\)
\(cn^k ≥ Ο(n^d)\)

证毕

其他证明同理，略。

### 3-2 (相对渐近增长)

为下表中的每对表达式 (A,B) 指出 A 是否是 B 的 Ο, ο, Ω, ω 或 Θ. 假设 k ≥ 1、ε > 0 且 c > 1 均为常量。回答应该以表格的形式，将“是”或“否”写在每个空格中。

| A             | B              | Ο   | ο   | Ω   | ω   | Θ   |
| ------------- | -------------- | --- | --- | --- | --- | --- |
| \(\lg^kn\)    | \(n^ε\)        | n   | y   | n   | y   | n   |
| \(n^k\)       | \(c^n\)        | n   | n   | n   | y   | n   |
| \(\sqrt{n}\)  | \(n^{\sin n}\) | n   | n   | n   | n   | n   |
| \(2^n\)       | \(2^{n/2}\)    | n   | y   | n   | n   | n   |
| \(n^{\lg c}\) | \(c^{\lg n}\)  | y   | n   | y   | n   | y   |
| \(\lg n!\)    | \(\lg n^n\)    | n   | n   | n   | y   | n   |

### 3-3 (根据渐近增长率排序)

a. 根据增长的阶来排序下面的函数，即求出满足

$$
g_1= Ω(g_2), g_2= Ω(g_3), ... , g_29= Ω(g_{30})
$$

的函数的一种排列

$$
g_1,g_2,...,g_{30}
$$

。把你的表划分成等价类，使得函数 f(n)和 g(n) 在相同类中当且仅当 f(n) = Θ(g(n))。

用行表示同一类函数，上下顺序表示低阶到高阶。

**解：**

- 常数级别

$$
1,n^{1/\lg n} = n^{\log_n 2} = 2
$$

- 多重对数级别

$$
\lg (lg^*n)
$$

$$
lg^*n
$$

$$
\lg^*(\lg n)
$$

$$
2^{\lg^*n}
$$

- 对数级别

$$
\lg^2 n
$$

$$
\ln {\ln n}
$$

$$
\sqrt {\lg n}
$$

$$
\ln n
$$

- 次方数级别

$$
n
$$

$$
n \lg n
$$

$$
n^2
$$

$$
n^3
$$

- 指数级别

$$
2^{\sqrt {2 \lg n}}
$$

$$
(\sqrt2)^{\lg n}
$$

$$
2^{\lg n},4^{\lg n}
$$

$$
(\frac {3}{2})^n,e^n,2^n
$$

$$
n⋅2^n
$$

$$
2^{2^n},2^{2^{n+1}}
$$

- 阶乘级别

$$
(\lg n)^{\lg n}
$$

$$
n^{\lg \lg n}
$$

$$
(\lg n)!
$$

$$
\lg (n!)
$$

$$
n!,(n+1)!
$$

b. 给出非负函数 f(n) 的一个例子，使得对所有在 (a) 部分中的函数 \(g_i(n)\), f(n) 既不是 \(Ο(g_i(n))\) 也不是 \(Ω(g_i(n))\)。

$$
n^{n^{(\sin n)}}
$$

说明：通过构建三角函数，获得 -∞ 到 ∞ 交替的指数，于是能够得到 0 到 ∞ 交替的值域。

### 3-4（渐近记号的性质）假设 f(m) 和 g(n) 为渐近正函数。证明或反驳下面的每个猜测。

**a. \(f(n) = Ο(g(n)) 蕴含 g(n) = Ο(f(n))\)**

反驳：

根据 Ο 记号的定义：

$$
Ο(g(n))={f(n):存在正常量 c 和 n_0，使得对所有 n \geq n_0，有 0 \leq f(n) \leq cg(n)}
$$

已知 \(f(n) \leq cg(n)\)

需证明 \(c_1f(n) \geq g(n)\) 即 \(f(n) \geq \frac {1}{c_1} g(n)\)

由于 c 可取任意正常量，令其为 \(\frac {1}{c_1}\)，则有 \(f(n) \leq \frac {1}{c_1} g(n)\)

无法证明。

**b. \(f(n)+g(n) = Θ(min(f(n),g(n)))\)。**

根据 Θ 记号的定义：

$$
Θ(g(n))={f(n):存在正常量 c_1、c_2 和 n_0，使得对所有 n \geq n_0，有 0 \leq c_1g(n) \leq f(n) \leq c_2g(n)}.
$$

反驳：

如果 h(n) 和 m(n) 不同阶，显然 \(f(n)+g(n) = ω(min(f(n),g(n)))\)

如果同阶：

令 \(h(n) = min(f(n),g(n)),则有 h(n) \leq f(n) 或 h(n) \leq g(n).\)

令 \(m(n) = f(n)+g(n),则有 h(n) \leq \frac {1}{2} m(n).\)

可得 \(2h(n) \leq m(n)\)，只能说明 h(n) 至多是 m(n) 的渐近下界。

即
\( m(n) = Ω(h(n))\)
\(f(n)+g(n) = Ω(min(f(n),g(n)))\)

**c. \(f(n)=O(g(n))蕴涵 \lg (f(n))= O(\lg(g(n)))，其中对所有足够大的 n，有 \lg(g(n)) \geq 1 且 f(n) \geq 1\)。**

前半段证明：

根据 O 记号定义，可得 \(f(n) \leq cg(n)\)

对数函数单调递增，于是：

\(\lg(f(n)) \leq \lg(cg(n))\)
\(\lg(f(n)) \leq c_1\lg(g(n))\)
\(\lg(f(n)) \leq O(\lg(g(n)))\)

后半段反驳：

没有条件能说函数 f(n) 和 g(n) 是单调递增的。如果函数当 n 大于某个值后，值域内的值总是小于 2 ，那么并不能使得要求的结论成立。

**d. \(f(n) = O(g(n)) 蕴涵 2^{f(n)} = O(2^{g(n)})\)。**

证明：

根据 O 记号定义，可得 \(f(n) \leq cg(n)\)

指数函数单调递增，于是：

\(2^{f(n)} \leq 2^{cg(n)})\)
\(2^{f(n)} \leq c_12^{g(n)}\)
\(2^{f(n)} \leq O(2^{g(n)})\)

得证

**e. \(f(n) = O((f(n))^2)\)。**

反驳：

需要证明
\(f(n) \leq c(f(n))^2\)
\(1 \leq cf(n)\)

无法证明

**f. \(f(n) = O(g(n)) 蕴涵 g(n) = Ω(g(n))\)。**

反驳：

\(f(n) \leq c_1g(n)\)

不能推导出

\(g(n) \leq c_2f(n)\)

**g. \(f(n) = Θ(f(\frac{n}{2}))\)。**

反驳：

需要证明：

\(c_1f(\frac{n}{2}) \leq f(n) \leq c_2f(\frac{n}{2})\)
\(c_1\leq \frac {f(n)}{f(\frac{n}{2})} \leq c_2\)

这里需要 \(f(n)\) 和 \(f(\frac{n}{2})\) 同阶，即相除结果为常数。

当 \(f(n)\) 为阶乘函数时，显然两者不同阶，也就找不到常量 \(c_1、c_2\)。

原等式不成立。

**h. \(f(n) + ο(f(n)) = Θ(f(n))\)。**

先说结论：成立。

证明：

根据定义：

\(c_1f(n) \leq f(n) + ο(f(n)) \leq c_2f(n)\)
\(c_1 \leq \frac {f(n) + ο(f(n))}{f(n)} \leq c_2\)
\(c_1 \leq 1+ \frac {ο(f(n))}{f(n)} \leq c_2\)

根据 ο 记号的定义可知：

$$
\lim_{n \to ∞} \frac {ο(f(n))}{f(n)} = 0
$$

所以能够找到常量 \(c_1、c_2\) 使得原等式成立。

### 3-5 (O 与 Ω 的一些变形)

某些作者用一种与我们稍微不同的方式来定义 Ω ；假设我们使用\(\overset{\infty}{Ω}\)(读作“Ω 无穷”)来表示这种可选的定义。若存在正常量 c，使得对无穷多个整数 n，有 \(f(n) \geq cg(n) \geq 0\),则称 \(f(n) = \overset{\infty}{Ω}g(n))\)。

**a. 证明：对渐近非负的任意两个函数 f(n)和 g(n)，或者 \(f(n) = O(g(n))\) 或者 \(f(n) = \overset{\infty}{Ω}g(n))\) 或者二者均成立，然而，如果使用 Ω 来代替\(\overset{\infty}{Ω}\)，那么该命题并不为真。**

证明：

根据 Ω 记号的定义：

$$
Ω(g(n))={f(n):存在正常量 c 和 n_0，使得对所有 n \geq n_0，有 0 \leq cg(n) \leq f(n) }
$$

两者的区别在于，Ω 的范围是达到规模 \(n_0\) 之后的所有，而\(\overset{\infty}{Ω}\) 的范围是无穷而非所有。

在规模 \(n_0\) 之后，\(\overset{\infty}{Ω}\) 范围是 Ω 范围的子集。

...

不证了！我感觉题目前半段是错的。

**b. 描述用 \(\overset{\infty}{Ω}\) 代替 Ω 来刻画程序运行时间的潜在优点与缺点。**

对运行时间，我们希望的是能够在一定范围内度量它。

\(\overset{\infty}{Ω}\) 的缺点在于：它不能保证所有的情况都是一样的运行时间范围，存在部分情况超过了这个记号所估计的运行时间。特别是，如果\(\overset{\infty}{Ω}\) 集合是稀疏的，那么其本身的估计意义就几乎不存在了。

优点：如果存在 \(\overset{\infty}{Ω}\)，那么就可能存在 Ω，这是一个有希望实现的研究优化的方向。

**c. 某些作者也用一种稍微不同的方式来定义\(O\)；假设使用\(O'\)来表示这种可选的定义。我们称\(f(n)=O'(g(n))\)当且仅当\(|f(n)| =O(g(n))\)。如果使用\(O'\)代替\(O\)但仍然使用\(Ω\)，定理 3.1 中的“当且仅当”的每个方向将出现什么情况？**

定理 3.1 原文：

$$
对任意两个函数 f(n) 和 g(n),我们有 f(n) = Θ(g(n))，当且仅当 f(n) = O(g(n)) 且 f(n) = Ω(g(n))。
$$

变化后式子如下：

$$
对任意两个函数 f(n) 和 g(n),我们有 |f(n)| = Θ(g(n))，当且仅当 f(n) = O'(g(n)) 且 |f(n)| = Ω(g(n))。
$$

说明：

显然，\(O'\)比\(O\)范围更大，包括了横轴对称的函数的情况。我们用绝对值说明渐近区间就行了。

**d. 有些作者定义\(\overset{～}{O}\)（读作软 \(O\)）来意指忽略对数因子的 \(O\)：**

$$
\overset{～}{O}(g(n)) = \{f(n):存在正常量 c，k和n_0，使得对所有 n \geq n_0，有 0\leq f(n) \leq cg(n)\lg^k(n)\}
$$

**用一种类似的方式定义\(\overset{～}{Ω}\)和\(\overset{～}{Θ}\)。证明与定理 3.1 相对应的类似结论。**

定义如下：

$$
\overset{～}{Ω}(g(n)) = \{f(n):存在正常量 c，k和n_0，使得对所有 n \geq n_0，有 0\leq cg(n)\lg^k(n) \leq f(n)\}
$$

$$
\overset{～}{Θ}(g(n)) = \{f(n):存在正常量 c_1，c_2，k和n_0，使得对所有 n \geq n_0，有 0\leq c_1g(n)\lg^k(n) \leq f(n) \leq c_2g(n)\lg^k(n)\}
$$

证明过程显而易见。

### 3-6 （多重函数）

我们可以把用于函数\(\lg^\ast\)中的重复操作符\(\ast\)应用于实数集上的任意单调递增函数\(f(n)\)。对给定的常量\(c \in R\)，我们定义多重函数\(f^\ast_c\)为：

$$
f^\ast_c = min\{i\geq 0:f^{(i)}(n)\leq c\}
$$

该函数不必在所有情况下都为良定义的。换句话说，值\(f^\ast_c(n)\)是为缩小其参数到 c 或更小所需要函数 f 重复应用的数目。对如下每个函数 f(n) 和常量 c，给出\(f^\ast_c(n)\)的一个尽量紧确的界。

| ID  | \(f(n)\)      | c   | \(f^\ast_c(n)\) |
| --- | ------------- | --- | --------------- |
| a   | \(n-1\)       | 0   | \(n\)           |
| b   | \(\lg(n)\)    | 1   | \(\lg(n)\)      |
| c   | \(n/2\)       | 1   | \(\lg(n)\)      |
| d   | \(n/2\)       | 2   | \(\lg(n)-1\)    |
| e   | \(\sqrt {n}\) | 2   | \(\lg(n)\)      |
| f   | \(\sqrt {n}\) | 1   | \(\lg(n)\)      |
| g   | \(n^{1/3}\)   | 2   | \(\lg(n)\)      |
| h   | \(n/\lg n\)   | 2   | ---             |

题目理解：求使得上式成立的 i 值。

(不会更紧确了)
